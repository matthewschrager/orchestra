{
  "name": "server",
  "version": "0.0.1",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "echo 'server runs via bun directly'"
  },
  "dependencies": {
    "hono": "^4",
    "shared": "workspace:*",
    "nanoid": "^5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
